class Trip {
    constructor(from, until, destination, participants) {
        this.from = from;
        this.until = until;
        this.destination = destination;
        this.participants = participants;
    }
}

module.exports = Trip;